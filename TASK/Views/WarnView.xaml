<Window x:Class="TASK.Views.WarnView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TASK.Views"
        mc:Ignorable="d"
        Title="Warning"
        Background="WhiteSmoke" Height="249.443" Width="546.797"
        FontSize="16" FontWeight="Bold">
    <Grid>
        <!-- Rows/Columns defenitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ROW 0 -->
        <TextBlock x:Name="Text" Margin="5,5,5,5" 
				   Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" 
				   VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>

        <!-- ROW 1 -->
        <Button x:Name="Ok"  Margin="20,7,20,20" 
                Grid.Row="1" Grid.Column="1">Ok</Button>
    </Grid>
</Window>
